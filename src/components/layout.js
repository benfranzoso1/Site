import React from "react"
import Logo from "../assets/equipmentloans.svg"
import Arrow from "../assets/arrow-thin-right.svg"

export default ({ children }) => (
  <div>
    <header></header>
    <main>{children}</main>
    <footer></footer>
  </div>
)
